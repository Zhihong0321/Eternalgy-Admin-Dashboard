# Database Connection (shared with sync system)
DATABASE_URL=postgresql://username:password@host:port/database

# Application Settings
NODE_ENV=production
PORT=3001

# Frontend URL (Railway auto-generated)
FRONTEND_URL=https://your-railway-frontend-url.up.railway.app

# Optional: Separate read-only database user
# DATABASE_READONLY_URL=postgresql://readonly_user:password@host:port/database

# Railway Environment Variables
# These will be automatically set by Railway:
# RAILWAY_STATIC_URL
# RAILWAY_PUBLIC_DOMAIN
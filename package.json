{
  "name": "eternalgy-admin-dashboard-2.0",
  "version": "1.0.0",
  "description": "Eternalgy Admin Dashboard 2.0 - Simple Node.js Backend",
  "main": "server.js",
  "type": "module",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "start": "npx prisma db push && npx prisma generate && node server.js",
    "dev": "node --watch server.js",
    "build": "npx prisma generate && cd frontend && npm install && npm run build",
    "build:backend": "npx prisma generate",
    "build:frontend": "cd frontend && npm install && npm run build",
    "build:full": "npm run build:backend && npm run build:frontend"
  },
  "author": "Eternalgy Team",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/Zhihong0321/Eternalgy-Admin-Dashboard.git"
  },
  "dependencies": {
    "@prisma/client": "^6.14.0",
    "express": "^4.18.2",
    "pg": "^8.16.3",
    "prisma": "^6.14.0"
  }
}

[phases.setup]
nixPkgs = ["nodejs-20_x", "npm-9_x"]

[phases.install]
cmds = ["echo 'Skipping install phase - using custom build script'"]

[phases.build]
cmds = ["chmod +x railway-build.sh && ./railway-build.sh"]

[start]
cmd = "npm start"

[variables]
NODE_VERSION = "20.19.0"